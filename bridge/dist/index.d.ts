#!/usr/bin/env node
/**
 * Zapista WhatsApp Bridge
 *
 * This bridge connects WhatsApp Web to Zapista's Python backend
 * via WebSocket. It handles authentication, message forwarding,
 * and reconnection logic.
 *
 * Usage:
 *   npm run build && npm start
 *
 * Or with custom settings:
 *   BRIDGE_PORT=3001 AUTH_DIR=~/.zapista/whatsapp-auth npm start
 */
export {};
